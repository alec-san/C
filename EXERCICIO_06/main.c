#include <stdio.h>
#include <stdlib.h>

// Disciplina   : Linguagem e Lógica de Programação

int main()
{
    int x, y, z;
    x = 9;
    y = 3;
    z = y - x;
    printf("z: %d \n", z);

    x = 2;
    y = x + z;
    printf("x: %d, y: %d, z: %d", x, y, z);

    return 0;
}
